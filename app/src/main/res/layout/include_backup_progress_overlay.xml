<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_backup_progress"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_app_card"
    android:elevation="12dp"
    android:orientation="vertical"
    android:padding="14dp"
    android:visibility="gone">

    <TextView
        android:id="@+id/text_backup_progress"
        style="@style/AppChromeCaption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="3"
        android:text="正在准备 WebDAV 操作..."
        android:textColor="?attr/themeColorAppTextPrimary" />

    <ProgressBar
        android:id="@+id/progress_backup"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginTop="10dp"
        android:indeterminate="true"
        android:indeterminateTint="?attr/themeColorAppPrimary"
        android:progressBackgroundTint="?attr/themeColorAppBorder"
        android:progressTint="?attr/themeColorAppPrimary" />
</LinearLayout>
